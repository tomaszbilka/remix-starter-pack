# Welcome to Remix Starting Pack!

##### Motivation:

It is repository which contains starter point for following stack:

- Remix.js
- Tailwind
- MUI (+ `zod` + `moment` + `react-hook-form`)
- react-i18next

## Running Locally

```sh
npm i
```

Then run the app in development mode:

```sh
npm run dev
```
